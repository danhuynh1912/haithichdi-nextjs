version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 20
        - npm ci
    build:
      commands:
        - rm -f .env.production
        - touch .env.production
        - env | grep -e NEXT_PUBLIC_ >> .env.production || true
        - env | grep -e SERVER_API_BASE_URL >> .env.production || true
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
